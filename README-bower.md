# update dependencies

    bower update

# Install bower:

    brew install npm
    npm install -g bower

# Install dependencies:

    bower init
    bower install angular --save
    bower install angular-bootstrap --save

