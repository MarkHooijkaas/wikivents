{{>header}}

  <table class="table table-hover">
    <thead>
      <tr>
        <th>Naam</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
{{#each model.users}}  
      <tr>
        <td>{{username}}</td>
        <td>{{email}}</td>
      </tr>
{{/each}}
    </tbody>
  </table>
  
{{>footer}}