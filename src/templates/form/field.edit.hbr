  <div class="form-group">
    <label for="{{field.name}}">{{label}}: <i>{{field.message}}</i></label>
    <input type="{{#if type}}{{type}}{{else}}text{{/if}}" class="form-control" id="{{field.name}}" name="{{field.name}}" value="{{field.value}}">
  </div>
  