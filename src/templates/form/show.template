{{>header}}

{{form.changeButton}}

  <table class="table table-hover">
    <thead>
      <tr>
        <th>Veld</th>
        <th>Waarde</th>
      </tr>
    </thead>
    <tbody>
{{#each form.fields}}
  {{&show}}
{{/each}}

    </tbody>
  </table>
 
{{>footer}}