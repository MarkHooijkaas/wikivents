{{>header}}
{{#if userMayChange}}
<form role="form" method="GET" action="/{{page.name}}/edit/{{record._id}}">
  <button type="submit" class="btn btn-default">Wijzig</button>
</form>
{{/if}}

  <table class="table table-hover">
    <thead>
      <tr>
        <th>Veld</th>
        <th>Waarde</th>
      </tr>
    </thead>
    <tbody>
{{#each form.fields}}
  {{&show}}
{{/each}}

    </tbody>
  </table>
 
{{>footer}}