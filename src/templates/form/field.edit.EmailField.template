  <div class="form-group">
    <label for="{{field.name}}">{{field.label}}</label>
    <input type="email" class="form-control" id="{{field.name}}" name="{{field.name}}" value="{{value}}">
  </div>
  