{{>header}}

  <table class="table table-hover">
    <thead>
      <tr>
        <th>Datum</th>
        <th>Organisator</th>
        <th>activiteit</th>
        <th>plaats</th>
      </tr>
    </thead>
    <tbody>
{{#each model.events}}  
      <tr>
        <td>{{date}}</td>
        <td>{{organizer.get.username}}</td>
        <td><a href="event/{{_id}}">{{title}}</a></td>
        <td>{{location}}</td>
      </tr>
{{/each}}
    </tbody>
  </table>
  
{{>footer}}