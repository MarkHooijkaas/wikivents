{{> header}}

<h1>{{user.username}}</h1>

  <table class="table table-hover">
    <tbody>
      {{> form/field.show field=form.username}}
      {{> form/field.show field=form.email}}
      {{> form/field.show field=form.password}}
    </tbody>
  </table>

{{> form/changeButton}}
  
{{> footer}}