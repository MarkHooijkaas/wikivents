{{> header}}

<h1>{{record.username}}</h1>

  <table class="table table-hover">
    <tbody>
      {{> show/text value=record.username label="Gebruikersnaam"}}
      {{> show/text value=record.email label="email"}}
    </tbody>
  </table>

{{> show/changeButton}}
 
{{> footer}}