{{> header}}

<h1>{{user.username}}</h1>

  <table class="table table-hover">
    <tbody>
      {{> form/field.show value=record.username label="Gebruikersnaam"}}
      {{> form/field.show value=record.email label="email"}}
    </tbody>
  </table>

{{> form/changeButton}}
  
{{> footer}}