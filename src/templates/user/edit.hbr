{{> header}}

<form role="form" method="post">
  {{> form/field.edit field=form.username label="Gebruikersnaam"}}
  {{> form/field.edit field=form.email    label="email"}}
  {{> form/field.edit field=form.password label="wachtwoord"}}
  
  {{> form/submitButton}}
</form>


{{> footer}}