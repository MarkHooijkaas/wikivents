{{> header}}

<form role="form" method="post">
  {{> form/field.edit field=form.username}}
  {{> form/field.edit field=form.email}}
  {{> form/field.edit field=form.password}}
  
  {{> form/submitButton}}
</form>


{{> footer}}