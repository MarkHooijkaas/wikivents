{{> header}}

<script > $(document).ready(function(){ $('#mydatepicker').datepicker() }); </script>


<form role="form" method="POST">
	{{> form/field.edit field=form.title}}
	{{> form/field.edit field=form.date}}
	{{> form/field.edit field=form.location}}
	{{> form/field.edit field=form.min}}
	{{> form/field.edit field=form.max}}
	{{> form/field.edit field=form.description}}

	{{> form/submitButton}}
</form>


{{> footer}}