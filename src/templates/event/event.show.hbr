{{> header}}

<h1>{{record.title}}</h1>
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Veld</th>
        <th>waarde</th>
      </tr>
    </thead>
    <tbody>
	{{> form/field.show field=form.date}}
	{{> form/field.show field=form.min}}
	{{> form/field.show field=form.max}}
	{{> form/field.show field=form.location}}
    </tbody>
  </table>

{{> form/changeButton}}
  
  <div>{{form.description.value}}</div>
  
{{> footer}}